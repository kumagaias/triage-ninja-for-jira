# TriageNinja for Jira - 設計書

## アーキテクチャ概要

### システム構成図

```
┌─────────────────────────────────────────────────────────┐
│                    Jira Cloud                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ Project Page │  │ Issue Panel  │  │ Issue Glance │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │
│         │                  │                  │         │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Forge Runtime  │
                    │   (Serverless)  │
                    └────────┬────────┘
                             │
          ┏━━━━━━━━━━━━━━━━━┻━━━━━━━━━━━━━━━━━┓
          ┃                                      ┃
    ┌─────▼─────┐                      ┌────────▼────────┐
    │   React   │                      │ Forge Functions │
    │  UI Kit   │                      │   (Node.js)     │
    └───────────┘                      └────────┬────────┘
                                                 │
                              ┏━━━━━━━━━━━━━━━━┻━━━━━━━━━━━━━━━━┓
                              ┃                                   ┃
                    ┌─────────▼─────────┐           ┌───────────▼──────────┐
                    │   Rovo Agent      │           │   Jira REST API v3   │
                    │  (AI Analysis)    │           │  (Issue/User/Search) │
                    └───────────────────┘           └──────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │  Forge Storage    │
                    │ (Data Persistence)│
                    └───────────────────┘
```

### コンポーネント構成

#### フロントエンド
- **Dashboard Component**: プロジェクトページのダッシュボード
- **Issue Panel Component**: チケット詳細パネル
- **AI Triage Modal**: AI分析結果表示モーダル
- **Statistics Cards**: 統計情報カード

#### バックエンド
- **Resolver Functions**: API呼び出しハンドラー
- **Rovo Agent Functions**: AI分析ロジック
- **Data Service**: Forge Storage操作
- **Jira API Client**: Jira REST API ラッパー

## データベース設計

### Forge Storage スキーマ

#### 1. triageHistory
チケットのトリアージ履歴を保存

```typescript
interface TriageHistory {
  ticketId: string;           // "HELP-1234"
  triageDate: string;         // ISO 8601形式
  category: string;           // "Network & Connectivity"
  subCategory: string;        // "VPN Issues"
  priority: string;           // "High" | "Medium" | "Low"
  urgency: string;            // "Urgent" | "Normal"
  assignedTo: string;         // "Alex Chen"
  confidence: number;         // 0-100
  aiVersion: string;          // "1.0"
  reasoning: string;          // 判定理由
  tags: string[];             // ["vpn", "remote-work"]
}
```

**キー**: `triage:history:{ticketId}`

#### 2. agentPerformance
担当者のパフォーマンスデータ

```typescript
interface AgentPerformance {
  agentName: string;          // "Alex Chen"
  agentId: string;            // Jira user ID
  categories: {
    [category: string]: {
      totalTickets: number;   // 150
      resolved: number;       // 143
      avgResolutionTime: string; // "25min"
      successRate: number;    // 0.95
    }
  };
  currentLoad: number;        // 現在の担当チケット数
  lastUpdated: string;        // ISO 8601形式
}
```

**キー**: `agent:performance:{agentId}`

#### 3. appSettings
アプリケーション設定

```typescript
interface AppSettings {
  projectKey: string;         // "HELP"
  enableAutoTriage: boolean;  // true
  confidenceThreshold: number; // 70
  maxSimilarTickets: number;  // 5
  categories: string[];       // カスタムカテゴリー
  lastSync: string;           // ISO 8601形式
}
```

**キー**: `app:settings:{projectKey}`

#### 4. similarityCache
類似チケット検索のキャッシュ

```typescript
interface SimilarityCache {
  ticketId: string;           // "HELP-1234"
  similarTickets: Array<{
    id: string;               // "HELP-892"
    similarity: number;       // 0.89
    solution: string;         // "Reset VPN cache"
    resolutionTime: string;   // "20min"
  }>;
  cachedAt: string;           // ISO 8601形式
  expiresAt: string;          // ISO 8601形式
}
```

**キー**: `similarity:cache:{ticketId}`

### データアクセスパターン

#### 読み取り
- チケットトリアージ時: `triage:history:{ticketId}`
- 担当者選定時: `agent:performance:*`
- 類似チケット検索時: `similarity:cache:{ticketId}`
- ダッシュボード表示時: `triage:history:*` (範囲検索)

#### 書き込み
- トリアージ完了時: `triage:history:{ticketId}` 作成
- チケット解決時: `agent:performance:{agentId}` 更新
- 類似チケット検索時: `similarity:cache:{ticketId}` 作成

## API設計

### Rovo Agent API

#### 1. チケット分類 (classify-ticket)

**入力**:
```typescript
interface ClassifyTicketInput {
  summary: string;
  description: string;
  reporter: string;
  created: string;
}
```

**プロンプト**:
```
以下のサポートチケットを分析し、適切なカテゴリー、優先度、緊急度を判定してください。

チケット情報:
- 概要: {summary}
- 詳細: {description}
- 報告者: {reporter}
- 作成日時: {created}

以下のJSON形式で回答してください:
{
  "category": "カテゴリー名",
  "subCategory": "サブカテゴリー名",
  "priority": "High/Medium/Low",
  "urgency": "Urgent/Normal",
  "confidence": 0-100の数値,
  "reasoning": "判定理由",
  "tags": ["タグ1", "タグ2"]
}
```

**出力**:
```typescript
interface ClassifyTicketOutput {
  category: string;
  subCategory: string;
  priority: "High" | "Medium" | "Low";
  urgency: "Urgent" | "Normal";
  confidence: number;
  reasoning: string;
  tags: string[];
}
```

#### 2. 担当者マッチング (suggest-assignee)

**入力**:
```typescript
interface SuggestAssigneeInput {
  category: string;
  subCategory: string;
  availableAgents: Array<{
    name: string;
    id: string;
    skills: string[];
    currentLoad: number;
  }>;
  historicalData: Array<{
    agent: string;
    category: string;
    avgResolutionTime: string;
    successRate: number;
  }>;
}
```

**プロンプト**:
```
以下の情報を基に、最適な担当者を選定してください:

カテゴリー: {category}
サブカテゴリー: {subCategory}
利用可能な担当者: {availableAgents}
過去の実績: {historicalData}

JSON形式で回答:
{
  "assignee": "担当者名",
  "assigneeId": "担当者ID",
  "reason": "選定理由",
  "confidence": 0-100,
  "estimatedTime": "推定解決時間",
  "alternatives": [
    {
      "assignee": "代替担当者名",
      "reason": "理由"
    }
  ]
}
```

**出力**:
```typescript
interface SuggestAssigneeOutput {
  assignee: string;
  assigneeId: string;
  reason: string;
  confidence: number;
  estimatedTime: string;
  alternatives: Array<{
    assignee: string;
    assigneeId: string;
    reason: string;
  }>;
}
```

#### 3. 類似チケット検索 (find-similar)

**入力**:
```typescript
interface FindSimilarInput {
  currentTicket: {
    summary: string;
    description: string;
  };
  pastTickets: Array<{
    id: string;
    summary: string;
    description: string;
    resolution: string;
    resolutionTime: string;
  }>;
}
```

**プロンプト**:
```
以下の現在のチケットに類似する過去のチケットを分析し、
最も関連性の高いものを選定してください:

現在のチケット:
- 概要: {summary}
- 詳細: {description}

過去のチケット:
{pastTickets}

JSON形式で回答:
{
  "similarTickets": [
    {
      "id": "チケットID",
      "similarity": 0-1の数値,
      "solution": "解決方法",
      "resolutionTime": "解決時間"
    }
  ],
  "suggestedActions": [
    "推奨アクション1",
    "推奨アクション2"
  ]
}
```

**出力**:
```typescript
interface FindSimilarOutput {
  similarTickets: Array<{
    id: string;
    similarity: number;
    solution: string;
    resolutionTime: string;
  }>;
  suggestedActions: string[];
}
```

### Jira REST API 統合

#### 1. チケット取得
```typescript
GET /rest/api/3/issue/{issueIdOrKey}
```

#### 2. チケット更新
```typescript
PUT /rest/api/3/issue/{issueIdOrKey}
Body: {
  fields: {
    assignee: { accountId: string },
    priority: { id: string },
    labels: string[]
  }
}
```

#### 3. チケット検索
```typescript
POST /rest/api/3/search
Body: {
  jql: string,
  maxResults: number,
  fields: string[]
}
```

#### 4. アサイン可能ユーザー取得
```typescript
GET /rest/api/3/user/assignable/search?project={projectKey}
```

## UI/UX設計

### 画面構成

#### 1. ダッシュボード (Project Page)

**レイアウト**:
```
┌─────────────────────────────────────────────────────┐
│  TriageNinja Dashboard                    🥷        │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │ 未処理   │ │ 本日処理 │ │ 時間削減 │ │ AI精度 ││
│  │   24     │ │   156    │ │   78%    │ │  94%   ││
│  └──────────┘ └──────────┘ └──────────┘ └────────┘│
├─────────────────────────────────────────────────────┤
│  未トリアージチケット                    [フィルター]│
│  ┌─────────────────────────────────────────────────┐│
│  │ HELP-1234  Cannot access VPN      High  [Triage]││
│  │ HELP-1235  Printer not working    Med   [Triage]││
│  │ HELP-1236  Password reset         Low   [Triage]││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

**コンポーネント**:
- StatisticsCard: 統計情報カード
- TicketList: チケット一覧
- FilterBar: フィルターバー
- TriageButton: トリアージボタン

#### 2. チケット詳細パネル (Issue Panel)

**レイアウト**:
```
┌─────────────────────────────────────────────────────┐
│  AI Triage                                    🥷    │
├─────────────────────────────────────────────────────┤
│  Status: Not Triaged                                │
│                                                      │
│  ┌─────────────────────────────────────────────────┐│
│  │         [🤖 Run AI Triage]                      ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  Last Triage: Never                                 │
└─────────────────────────────────────────────────────┘
```

**トリアージ後**:
```
┌─────────────────────────────────────────────────────┐
│  AI Triage Results                            🥷    │
├─────────────────────────────────────────────────────┤
│  Confidence: ████████████░░░░░░░░ 94%              │
│                                                      │
│  📁 Category                                        │
│     Network & Connectivity > VPN Issues             │
│                                                      │
│  ⚡ Priority: High                                  │
│  🚨 Urgency: Urgent                                 │
│                                                      │
│  👤 Suggested Assignee                              │
│     Alex Chen                                       │
│     ✓ VPN specialist, 95% success rate             │
│     ⏱️ Avg resolution: 25min                        │
│     📊 Current load: 5 tickets                      │
│                                                      │
│  🔍 Similar Tickets (3)                             │
│     HELP-892 (89% similar) - Reset VPN cache        │
│     HELP-756 (85% similar) - Update client          │
│     HELP-623 (82% similar) - Check firewall         │
│                                                      │
│  ┌──────────────┐  ┌──────────────┐                │
│  │   [Approve]  │  │   [Reject]   │                │
│  └──────────────┘  └──────────────┘                │
└─────────────────────────────────────────────────────┘
```

#### 3. AI分析モーダル

**ローディング状態**:
```
┌─────────────────────────────────────────────────────┐
│  Analyzing Ticket...                          ✕     │
├─────────────────────────────────────────────────────┤
│                                                      │
│                    🥷                                │
│                  ⚡ ⚡ ⚡                             │
│                                                      │
│         AI is analyzing your ticket...              │
│                                                      │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░ 45%         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### カラースキーム

```css
/* Primary Colors */
--ninja-primary: #6366F1;      /* Indigo */
--ninja-secondary: #8B5CF6;    /* Purple */
--ninja-accent: #EC4899;       /* Pink */

/* Status Colors */
--success: #10B981;            /* Green */
--warning: #F59E0B;            /* Amber */
--error: #EF4444;              /* Red */
--info: #3B82F6;               /* Blue */

/* Confidence Colors */
--confidence-high: #10B981;    /* 90%+ */
--confidence-medium: #F59E0B;  /* 70-89% */
--confidence-low: #EF4444;     /* <70% */

/* Neutral Colors */
--gray-50: #F9FAFB;
--gray-100: #F3F4F6;
--gray-200: #E5E7EB;
--gray-300: #D1D5DB;
--gray-400: #9CA3AF;
--gray-500: #6B7280;
--gray-600: #4B5563;
--gray-700: #374151;
--gray-800: #1F2937;
--gray-900: #111827;
```

### アイコン

Lucide Reactを使用:
- 🥷 Ninja (ロゴ): `Zap` + カスタムスタイル
- 📁 Category: `Folder`
- ⚡ Priority: `Zap`
- 🚨 Urgency: `AlertCircle`
- 👤 Assignee: `User`
- 🔍 Similar: `Search`
- ✓ Success: `CheckCircle`
- ✕ Close: `X`
- ⏱️ Time: `Clock`
- 📊 Stats: `BarChart`

## セキュリティ設計

### 認証・認可
- Forge platformの認証機構を使用
- Jira権限に基づくアクセス制御
- プロジェクト管理者のみ設定変更可能

### データ保護
- Forge Storage使用（Runs on Atlassian準拠）
- データは暗号化して保存
- 個人情報は最小限に保持

### API セキュリティ
- APIキーは環境変数で管理
- レート制限の実装
- エラーメッセージに機密情報を含めない

## パフォーマンス最適化

### キャッシング戦略
- 類似チケット検索結果を24時間キャッシュ
- 担当者パフォーマンスデータを1時間キャッシュ
- ダッシュボード統計を5分キャッシュ

### 非同期処理
- AI分析は非同期で実行
- ローディング状態を明確に表示
- タイムアウト処理（30秒）

### データ最適化
- 必要なフィールドのみ取得
- ページネーション実装
- 不要なデータは削除

## エラーハンドリング

### エラー種別

#### 1. API エラー
```typescript
class APIError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public details?: any
  ) {
    super(message);
  }
}
```

#### 2. AI 分析エラー
```typescript
class AIAnalysisError extends Error {
  constructor(
    public reason: string,
    public ticketId: string
  ) {
    super(`AI analysis failed: ${reason}`);
  }
}
```

#### 3. データエラー
```typescript
class DataError extends Error {
  constructor(
    public operation: string,
    public key: string
  ) {
    super(`Data operation failed: ${operation} on ${key}`);
  }
}
```

### エラー表示

```typescript
interface ErrorDisplay {
  title: string;
  message: string;
  action?: {
    label: string;
    handler: () => void;
  };
}
```

**例**:
```
┌─────────────────────────────────────────────────────┐
│  ⚠️ Analysis Failed                                 │
├─────────────────────────────────────────────────────┤
│  Unable to analyze ticket due to insufficient       │
│  information. Please add more details to the        │
│  ticket description.                                │
│                                                      │
│  ┌──────────────┐                                   │
│  │   [Retry]    │                                   │
│  └──────────────┘                                   │
└─────────────────────────────────────────────────────┘
```

## テスト戦略

### ユニットテスト
- Forge Functions: Jest
- React Components: React Testing Library
- カバレッジ目標: 80%以上

### 統合テスト
- Jira API統合テスト
- Rovo Agent統合テスト
- Forge Storage操作テスト

### E2Eテスト
- チケットトリアージフロー
- 担当者アサインフロー
- ダッシュボード表示

## デプロイメント

### 環境
- Development: ローカル開発環境
- Staging: Forge開発環境
- Production: Atlassian Marketplace

### デプロイフロー
```bash
# 開発
forge tunnel

# ビルド
forge build

# デプロイ
forge deploy

# インストール
forge install
```

## 更新履歴

| 日付 | バージョン | 変更内容 | 担当者 |
|------|-----------|---------|--------|
| 2025-12-11 | 1.0 | 初版作成 | - |
